import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "RC Verification - Check Vehicle Registration",
  description: "Verify vehicle registration certificate details online. Check RC status, ownership history, and more on Nxcar.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
